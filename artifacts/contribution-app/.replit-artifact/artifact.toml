kind = "web"
previewPath = "/"
title = "AjoTrack"
description = "A digital passbook app for cooperative/Ajo/Thrift savings societies. Members log in to view their own contribution history and running balance. Admins manage all members, record credit/debit transactions, and export financial reports."
version = "1.0.0"
id = "artifacts/contribution-app"
router = "path"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21192

[services.development]
run = "pnpm --filter @workspace/contribution-app run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/contribution-app", "run", "build" ]
serve = "static"
publicDir = "artifacts/contribution-app/dist/public"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21192"
BASE_PATH = "/"
